node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
todos.json
mcp-todos.json
.kilo
.gemini
.trae
.commandcode
.playwright-mcp
.vscode
.zcode
/docs
# Local pnpm override that links the sibling react-animejs-package for dev.
# Kept out of git so it doesn't impose a directory layout on other contributors.
pnpm-workspace.yaml